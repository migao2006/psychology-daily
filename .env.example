# Optional OpenAlex API key for higher quota. Never expose this file to the browser.
OPENALEX_API_KEY=
# Required by the Unpaywall API to identify the automated client.
UNPAYWALL_EMAIL=
# Supported values: groq, gemini, or openai.
LLM_PROVIDER=
# Choose a structured-output-capable model in your selected provider account.
LLM_MODEL=
# Configure exactly one matching provider key as a GitHub Actions secret.
GEMINI_API_KEY=
OPENAI_API_KEY=
GROQ_API_KEY=
